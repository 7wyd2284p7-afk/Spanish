# Spanish 项目维护说明

本文件供维护该仓库的 AI 与开发者使用。用户当次提出的明确要求优先于本文件。

## 项目目标

为中文母语、从零开始的学习者 Penny 提供轻压力、可持续、真正能用于交流的西班牙语课程。课程从 A1 逐步扩展到 A2、B1，但任何扩展都不能破坏现有 A1 进度和练习体验。

## 开始修改前

1. 阅读 `README.md` 和 `COURSE_SPEC.md`。
2. 查看 `git status`，保留用户尚未提交的修改。
3. 确认任务属于课程内容、界面、交互逻辑还是发布，不要把不同职责重新混入同一文件。

## 文件职责

- `data/course-*.js`：课程事实、教学顺序、答案和反馈，是课程内容的唯一来源。
- `scripts/`：渲染、练习、存储和交互逻辑，不承载具体课程正文。
- `styles/`：视觉样式与响应式布局。
- HTML：语义结构、页面入口和资源引用，避免重新内嵌大量 CSS 或 JavaScript。
- `COURSE_SPEC.md`：课程标准和路线。改变教学方法或级别范围时同步更新。
- `README.md`：面向使用者和维护者的当前真实状态。改变入口、结构或运行方式时同步更新。

## 教学硬规则

- 中文解释要自然、简短、准确；西班牙语必须包含正确的重音符号和标点。
- 使用高频、现代、跨地区可理解的表达；遇到西班牙与拉美差异时简短标注，不强迫学习者一次记住多个变体。
- 不设置自由输入式拼写题。优先使用听辨、识别、选择、配对、词块排序和情境回应。
- 每节微课只引入一个主要语法重点和 4–6 个核心词块。
- 先提供可理解输入，再要求输出；新内容需要在后续课程中间隔复现。
- 错误反馈说明正确表达为什么适合当前情境，不使用责备式语言。
- 课程目标以“学完能完成什么交流任务”表述，不以术语记忆量表述。
- 不声称课程或测验提供官方 CEFR 认证。

## 课程数据约束

每节课保留稳定且唯一的 `id`。已发布的 ID 和 `localStorage` 键不得改名；必要的数据迁移必须兼容旧版本。

新增课程应延续以下字段：

```js
{
  id, title, minutes, canDo,
  words: [{ es, zh, tip }],
  grammar: { title, body, examples },
  sentence: { es, zh },
  order: { tokens, answer, zh },
  dialogue: { context, prompt, choices, answer, note }
}
```

所有选择题必须只有一个明确正确答案。`order.tokens` 按顺序连接后必须与 `order.answer` 完全一致，标点和空格也要一致。

## 产品与代码规则

- 网站保持无需构建、无需登录、离线可用；除非用户明确要求，不增加框架、后端、追踪代码或外部依赖。
- 保留键盘操作、可见焦点、语义标签和 `prefers-reduced-motion` 支持。
- 不把个人信息、密钥或学习记录提交到仓库。
- 修改数据模型时先设计向后兼容迁移；不要静默清空学习进度。
- 优先做小而完整的改动，避免为了重构改变已经工作的教学行为。

## 完成修改前

1. 运行 `node tests/validate-course.js`，并检查修改过的 JavaScript 语法。
2. 验证每个页面引用的本地文件都存在。
3. 在桌面和手机尺寸检查首页、课程列表、练习过程与总结页。
4. 手动走完至少一节受影响课程，并刷新确认进度保存。
5. 更新相关文档，说明新增了什么以及下一步仍缺什么。
6. 提交信息要描述用户可感知的变化；未经用户要求不要改为公开仓库。
